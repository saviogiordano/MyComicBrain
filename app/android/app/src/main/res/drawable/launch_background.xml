<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <!-- Nuvoletta dell'app icon, senza lo sfondo a pois (pensato per
         l'home screen, non per uno splash che deve fondersi col resto
         dell'esperienza scura) — riusa il layer foreground dell'adaptive
         icon già presente in ogni densità mipmap. -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher_foreground" />
    </item>
</layer-list>
