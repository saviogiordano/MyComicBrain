<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <!-- Icona completa dell'app (nuvoletta + retino), con angoli
         arrotondati come sull'home screen. Asset dedicato (non il layer
         foreground dell'adaptive icon): quello è ritagliato per la safe
         zone e risulterebbe minuscolo qui, dove non c'è nessuna maschera
         a limitarne le dimensioni. -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/splash_logo" />
    </item>
</layer-list>
