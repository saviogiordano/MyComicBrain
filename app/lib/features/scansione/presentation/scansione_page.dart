import 'package:flutter/material.dart';

class ScansionePage extends StatelessWidget {
  const ScansionePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scansione')),
      body: const Center(child: Text('Scansione')),
    );
  }
}
