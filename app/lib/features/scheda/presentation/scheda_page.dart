import 'package:flutter/material.dart';

class SchedaPage extends StatelessWidget {
  const SchedaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scheda')),
      body: const Center(child: Text('Scheda')),
    );
  }
}
