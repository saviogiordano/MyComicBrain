import 'package:flutter/material.dart';

class RicercaPage extends StatelessWidget {
  const RicercaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cerca')),
      body: const Center(child: Text('Cerca')),
    );
  }
}
