import 'package:flutter/material.dart';

class DuplicatiPage extends StatelessWidget {
  const DuplicatiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Duplicati')),
      body: const Center(child: Text('Duplicati')),
    );
  }
}
